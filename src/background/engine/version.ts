/** Bump on any change to a question, a constant or the combination: it enters the cache key (spec §6.2–6.3). */
// 1.1.0 (2026-09-25): round two in parts of 110; a claim's role shown only when confident; the pick worded by the
// top non-background role
export const ENGINE_VERSION = '1.1.0'
